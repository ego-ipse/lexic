"""Eidolon — ideal form: what a thing's shape IS, before it is drawn.

Two duties, both opsis's own rather than lexic's. :mod:`layout` turns
the naming graph into positions — a tidy tree, parents centred over
what they read. :mod:`topology` answers what a grammar's rules refer to
and how deep each sits, which is what a rules window is drawn FROM.

Nothing here reads a text. Deciding where a node sits is opsis's
business; deciding what a text means never is.
"""
