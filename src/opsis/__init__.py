"""Opsis — the visual instrument for lexic.

Lexic has one fundamental relation — ``product = read(reader, text)`` —
and opsis renders nothing else. A session is a ladder of rungs; the
surface is one node substance; the scene itself is IR. Opsis does not
compute: lexic computes, and every claim drawn is a computed truth or an
honest, drawn refusal.
"""
