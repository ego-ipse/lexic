# `lexic.ir.grammar.transform`

Whole-grammar transformations and relations: canonical form, name alignment,
and deterministic reference order. These modules depend on grammar nodes and
the action algebra and are re-exported through `lexic.ir`.
