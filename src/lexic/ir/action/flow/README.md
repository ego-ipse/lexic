# `lexic.ir.action.flow`

Value computation and control-flow actions: pure transformations in
`compute.py`, and sequencing/branching in `control.py`. Both sit on the action
and spine layers and are re-exported from `lexic.ir`.
