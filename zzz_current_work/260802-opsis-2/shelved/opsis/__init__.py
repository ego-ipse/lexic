"""Opsis — the visual instrument for lexic.

Lexic has one relation, ``product = read(reader, text)``, and opsis
holds nothing else: a session is a set of readings that name each other
as reader. Everything on screen follows from that — a tree is naming, a
lane is one text read twice, a plug is a rename.

Opsis does not compute. Lexic computes; opsis invokes it and draws what
came back, and every claim is a computed truth or an honest, drawn
refusal.
"""
