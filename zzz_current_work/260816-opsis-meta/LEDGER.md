# LEDGER — 260816-opsis-meta

Status only; the position lives in VISION.md.

## 2026-08-16 — zzz untracked on the user's go (`24130a7`)

All 409 tracked zzz_current_work files removed from the index only
(`git rm -r --cached`); every working-tree file verified on disk,
`git ls-files zzz_current_work` = 0, status clean. The branch now
carries src, tests, resources and wiki alone. The user pushes/merges
opsis_proto to main next; opsis-built-anew starts on a fresh branch
after.

## 2026-08-16 — transpile-mock density pass; all owed items closed

The last owed density pass applied to mock/transpile_room.html: the
table facet now carries 7 real rows with 8 folded behind a count and
its completeness verdict stated over the 15 drawable rules (T7's own
domain), and the caption carries the measured crossing mass (178 source
occurrences over 71 objects → 33 built, 14 table-born). Every mock now
meets the mass law; the implementer holds quietly; nothing is owed.
The effort stands at the user's gate: untrack + merge.

## 2026-08-16 — PHASE COMPLETE: all seven asks landed and committed

T7 committed `34baeaa`, gate independently EXIT=0 — the ask phase is
done. Eight commits: b03433e (seam family + generate refusals) ·
6b32114 (max_depth by rule heights) · a341801 (addressed emission) ·
baad3f5 (templating spans + Crossing) · 1325654 (verdict/census/
alignment) · ddefc59 (moments + census-sees-mappings) · 62bd57a
(kernel trace) · 34baeaa (presentation tables + markdown.gbnf corpus
citizen). 4148 tests passing (+~420 this phase). T7 notables: Row
field-set pinned against geometry vocabulary (B3); ceilings walk on
emit_addressed extents (cannot disagree with co-selection);
completeness over DRAWABLE rules; helper routing by moment
set-difference (two-parent helpers refuse); IrRenaming.rekeyed
transports any rule-keyed table; the md fixture's naive form was
genuinely ambiguous — caught by running both engines, restrictions now
stated in the grammar. AWAITING USER GO: untrack 409 zzz files, merge
to main, fresh branch for opsis-built-anew.

## 2026-08-16 — T7 confirmed running; value-room density pass

Crossed messages resolved (T6 was committed 62bd57a; T7 order restated
and begun). Density pass applied to mock/value_room.html per the mass
law: THE SHAPE now carries 8 real rules with the remaining 24 folded
behind a count; THE IDENTITY's share table extended with real kinds
(literal/ref/charclass arrivals, "12 more shared ▸ by arrivals") plus
the census-amendment fact drawn — a compiled fold's 35 IrLambda
refusals — so the boundary row teaches what T4b actually measures.
Transpile-mock density pass still owed. Remaining phase work: T7 report
→ review/commit → user's go on untrack + merge.

## 2026-08-16 — T6 committed; T7 (final task) dispatched

Committed `62bd57a`, gate independently EXIT=0. parsing/trace.py:
WatchedRun over TraceEvent(order, kind, rule, verdict, IrSpan) — shared
leaves held; pay-to-watch ENFORCED three ways (import arrow: nothing
under pda/ imports trace; structural gate: no PdaKernel code object
names the watch vocabulary; guarded interleaved A/B: 286 µs unwatched
solo AND interleaved, 541 µs watched, 1.89×). Deviations endorsed:
scans = text between decisions (tiling gate), no always-True rerun
field (shape carries it), refused runs return streams ending in the
refusal. T7 dispatched — the last task; md fixture grammar to be
authored into resources/ground_truth/. After T7: untrack + merge on
the user's explicit go. Owed next window: density passes on the early
sparse mocks (value room, transpile).

## 2026-08-16 — compilation-room mock (from T5's product, at mass)

`mock/compilation_room.html`: the moments strip as the room's clock
(rule counts 32→36→39 as passes mint helpers; the RESOLVED no-op dashed
with its reason — no_ops() drawn); the diff facet washing minted arms
red/green with the unchanged 29 folded behind a count (mass law); the
binding table with every row a hit into the value room; the caption
pinning ddefc59's guarantee — the artefact cannot disagree with the
strip because its classes ARE readings of it. T6 implementer nudged
(second idle-without-start; same pattern as spawn).

## 2026-08-16 — T5 + census amendment committed; T6 dispatched

Committed `ddefc59`, gate independently EXIT=0. Census now walks
mapping entries (reducers 5→272/843 nodes; json fold's true refusal
boundary: 35 IrLambda constructors, gated; flavour reducers honestly 0
— no defs in reductions); module moved to ir/identity.py under the
spine README's import law. T5: moments.py retains the whole pipeline;
build_codegen_grammar IS the chain's one spelling; no_ops() first-class
and probe-matched; CompiledGrammar.classes/codegen_grammar are now
properties over moments — artefact/moments disagreement unrepresentable.
T6 dispatched (shared-leaves + pay-to-watch + guarded in-process A/B
constraints restated; machine_room mock named as target). Compilation-
moments mock owed next window alongside the density passes.

## 2026-08-16 — the mass correction; artefact/compiled-files mock

User correction accepted: the mocks were chrome-first (five capsules)
where the parsing room is mass-first (12,219 spans) — a sparse mock is
a false reference that breeds sparse rooms. Pinned as ROOMS.md law 7
(a facet shows the subject's TRUE MASS; mock acceptance includes mass).
Drawn at mass: `mock/artefact_room.html` — the compiled-files room:
six-slot family with the measured numbers, payload literals as mass
strips (51,882 codes · 24,102 text · 8,441 shapes), the twin's whole
39-class roster with codegen-minted vs authored distinguished, verbatim
witnesses with their ~900 ms/790 KB costs said (why witnesses are
room-entry work). Density pass owed to value-room and transpile mocks,
queued for a later window.

## 2026-08-16 — T4 committed; census amendment ordered with T5

T4 reviewed and committed (`1325654`), gate independently EXIT=0:
Verdict on the compile seam (no prober/registry), census() identity
walk, align_names() with all bijections as transporting IrRenamings
(cross-flavour: json.gbnf ≅ json.abnf via identity witness). The
implementer's IrMapping-as-leaf flag ruled a SPEC BUG by the reviewer
(eidolon walked mapping entries; a reducer censusing as a childless
leaf is the old invisibility); amendment ordered before T5: child
definition = field-tuple + mapping entries as keyed children, reducer
census test must reach the IrLambdas. T5 dispatched to the same agent.

## 2026-08-16 — transpile-room mock (drawn from the real Crossing)

`mock/transpile_room.html`: source and target banks with object-level
co-selection through the Crossing's two addressed emissions ("local"
selected — its span left, its built occurrence right, one value in two
languages); the three gates as masthead verdicts on the relation; the
table as a docked, enterable subject whose rows wash what they read and
built; table-born models in a violet no-source tone (the honest
empty-set from baad3f5), never faked as correspondence. T4 implementer
nudged and running. Mock set: navigation, value room, ring, machine
room, rendered files, transpile room.

## 2026-08-16 — 3c/3d committed; T4 dispatched to a fresh implementer

Both re-scopes landed and committed (`baad3f5`), gate independently
EXIT=0. 3c's findings: the PDA already computed and discarded absolute
offsets; shared ParseTree nodes are provably zero-width, so tree-route
accumulation is exact; landed as a `span` fold mode on (slot, mode)
pairs — ordinary grammars pay nothing. 3d: Transpiler.run returns a
Crossing (both addressed emissions + IrOrigin per licensed pair;
unique/shared = set size; interior-constructed models honestly
empty-set, class names pinned). T3 fully closed. The T1–T3 agent
exhausted its context and handed over honestly; T4 dispatched to a
fresh Opus implementer (t4-implementer) against spec_plan T4 +
T3_REPORT handover. Transpile-crossing mock still queued.

## 2026-08-16 — rendering determination pinned (INTERFACES §5c)

"How is rendering determined" answered as a six-step chain of computed
licences and explicit casts, no heuristics: extension → pure readers;
reader probed never picked (multi-accept = offered cast); row tables
apply by GRAMMAR IDENTITY (bake + gates, rename-aligned), never by file
extension; precedence docked → policy-bound (the ring holds default
row-bindings — settings-editable) → shipped → none; the floor as the
guaranteed hole-free else (a gate-failing table is a refused offer,
never a partial ceiling); within a drawing, rule row → MRO floor →
raising default. New idea surfaced: the policy record carries per-
grammar-identity row bindings, making default renderings themselves
ring-editable data.

## 2026-08-16 — rendered-files mock (the universal-rendering picture)

`mock/rendered.html`, answering "how does a file render": THE RENDERED
is one more facet of the reading, never a mode — source and rendering
side by side, one cursor through T3 extents, typing in a rendered
region splices text and re-reads. S1: markdown with a docked row table
(itself a value/subject). S2: the FLOOR for a language with no authored
rows (structure over spelling, instantly addressable, dock offers
authoring — never fakes a ceiling); a grammar file's rendering
re-understood as the reader facet (the parsing room was the grammars'
ceiling all along); the all-readers-refuse room stays honest. Design
consequence recorded: rendered-region editing rides the same T3
region→span mapping — no new mechanism.

## 2026-08-16 — machine-room mock (T6 target drawn)

`mock/machine_room.html`: the trace facet as an ordered event stream
(scan/probe/rollback/gate, kinds toned by the register), the masthead
stating the cost model's two honesty facts (watched = RE-RUN; cap and
whether it bit), the scrubber, and trace↔grammar↔document co-selection
through the very IrSpan the event carries — the shared-leaves ruling
drawn. This is the T6 acceptance picture; until T6 lands the room draws
clone frames and says so. Mock set now covers: navigation/map, value
room, ring/settings, machine room. Remaining visual gaps: transpile
room's document crossing, compilation moments, artefact ring in room
context (partially covered by navigation's orbit), end_state refresh.

## 2026-08-16 — settings doctrine pinned; ring mock

User asked how settings are accessed/viewed/edited. Answered as doctrine
in INTERFACES.md §5b: no settings dialog — the ring. The policy record
is a document under policy.gbnf (no privileged format for opsis's own
config); `◌ ring` opens it from every masthead; save APPLIES. Three
tiers, one subject: direct (controls/seam-drags PATCH the record),
structural (value-room slots, typed offers), textual (re-reading with
refusals that leave the running policy untouched). Content addressing
gives settings history/revert/diff free; room state stays per-relation,
outside the record. Drawn as `mock/ring.html` (record plane with the
pad-999 refusal, slot offers with swatches, the policy trail with
supersession). Machine-room mock remains queued.

## 2026-08-16 — T3 slice committed; re-scopes approved; value-room mock

T3a/3b/3e reviewed and committed (`a341801`) — both gates independently
green; spans.py's docstring carries the occurrence contract verbatim;
3e verdict byte-identical (44 cases, 840 spans). The 3c/3d blockers
re-scoped by the reviewer and approved by the user: 3c = offsets by
accumulation over the fold's own leaves (search stays forbidden; a
leaf-text loss in the -sk twins would re-block); 3d = object-level
correspondence + both addressed emissions, wash-all-occurrences
semantics, occurrence IrOrigin only where unique — no new driver, no
silent pick. Agent continues 3c/3d then T4 under contest-or-proceed.
Second mock drawn: `mock/value_room.html` — S1 a grammar as subject
(SHAPE/SPELLING/IDENTITY, one cursor in three coordinate systems, the
×193 shared-quantifier wash), S2 the SLOTS anti-textarea with typed
algebra offers, the floor row for unknown kinds, the honest refusal.
Machine-room mock queued next window.

## 2026-08-16 — build-anew ruled; visual-reference inventory; first mock

User ruled: src/opsis is built ANEW; space_3 frozen as reference/quarry
(recorded in spec_plan roadmap). Visual-reference inventory answered
honestly: reading room = space_3 live (real reference); other rooms =
mock/end_state.html only (eleven static screens, adversarially shredded,
pre-dating the geometry/side-by-side/dock rulings — stale); navigation,
the orbit, the trail, and the value room's authored IR shapes = NO
visual reference at all. Gap-closing started: `mock/navigation.html`
drawn (S1 the map's three simultaneous projections with the orbit
focus-local, the dock as viewing, honest refusal chrome; S2 the hand's
law — focus/shift/spin as chrome states). Queued next windows: the value
room mock (the missing IR reference), then the machine room at T6 event
granularity.

## 2026-08-16 — ROOMS.md drafted (second T3 window)

The rooms catalogue: every room as a shell declaration (focus × facets ×
gestures × dock), each facet naming the engine surface it draws from by
T-number, per-room gate facts, six cross-room laws, and the ask-coverage
map closing the loop ("nothing consumes nothing; nothing waits on
anything unbuilt"). The artefact room's geometry facet IS the orbit
projection; the machine room's leaves are T6-on-T3 as ruled. Standing
instruction recorded: iterate on a design part during every subagent
window. Proposal status — not ruled.

## 2026-08-16 — INTERFACES.md drafted (while T3 runs)

The interface layer explored on the user's invitation: the wire grown to
speak the six elements (`#CAPS/#EDGES/#DOORS/#VERDICTS/#CURSOR` blocks —
T3's address records as the wire's address spelling, the shared-leaves
rule reaching the leaf); the shell contract (Room/Facet as declarations;
a room with zero gesturing facets fails the gate by construction); Q1's
travel gestures concretized (dive folds into enter; spin on the drawn
ring; shift = clicking an edge, distinct from focus by chrome); the map
as three simultaneous projections (lanes · focus-local orbit · trail)
under the tesseract rule; the viewing docked in the masthead with zero
new UI concepts (ports are edges, attachments capsules, licences
verdicts); leaf requirements grown (one-frame cursor wash); the src
package shape (five modes, sharpened charges). Three new questions
queued at its §8. Proposal status — not ruled.

## 2026-08-16 — scope correction and the max_depth fix

User correction: "out of scope" was the reviewer's unilateral scope cut —
the order was ALL asks. spec_plan gains T6 (kernel trace, on the ruled
shared-leaves design) and T7 (presentation table mechanism — ask #2's
lexic half; only the pixel solve stays opsis-side per B3). Second
correction applied as policy: bugs are fixed when found, not logged.
`generate.py`'s max_depth (threaded, never read) fixed by the reviewer
directly (commit `6b32114`): rule heights by fixpoint, looping arms dead
at every depth, exhausted budget restricts to minimal-height arms and
collapses quantified refs to lo, all-looping rules refuse with words,
dangling refs keep their named refusal. 33 generate tests green; the one
transient full-suite failure is the T3 agent's in-flight spans.py (its
CLAUDE.md line lands with its change). T1+T2 committed earlier as
`b03433e`.

## 2026-08-16 — implementation begins

User orders recorded: implement the asks; then untrack zzz_ from the
index; merge the lexic changes to main; start opsis on a fresh branch.
Commit grant given for meaningful points (no push, no co-author line).
`asks/spec_plan.md` written — T1 seam family · T2 generate refusals ·
T3 addressed emission/provenance · T4 verdict/identity-walk/alignment ·
T5 compile moments; asks #2 and #6 recorded out of scope; plan-time
decisions D1 (code-unit offsets), D2 (`ir/text/spans.py`), D3
(trace-leaves constraint). The former adversarial agent (Opus, warm with
full findings context) dispatched as implementer on T1+T2; reviewer
(main agent) reviews diffs, runs the gate independently, and commits.

## 2026-08-16 — user corrections on the adversarial fold-in

Two repairs to revision 2: (1) B1's phrasing corrected — an occurrence IS
a node (path, value) of the drawn graph; what it is not is the value
object alone; the address contract unchanged (VISION §4). (2) B2's wall
softened — a pure rename of topologically identical grammars is no real
boundary; a names-abstracted equality with a computed rule-name bijection
(the alignment witness) is added to ask #3 and transports rule-keyed
tables across renamings; alignment ambiguity is offered, never picked;
the honest refusal is reserved for genuine structural difference. M7
dissent elaborated into a proposed middle position: separate products,
shared leaves — the kernel trace keeps its own event-stream shape and
cost model but references ask #1's address/span records, so co-selection
composes without translation. M7 RULED same day: middle position
accepted — separate products, shared leaves. No dissents remain open;
src/lexic work is unblocked pending two plan-time micro-rulings (Q3
measure function; ask #1 module placement).

## 2026-08-16 — effort opened

## 2026-08-16 — probe round, adversarial round, revision 2

Rulings recorded: side-by-side projections (former Q2), md+JSON+ABNF as
the demonstrator ceilings (former Q5), src/lexic first (former Q7), and
the tesseract rule added to VISION §5 (four axes never fit one drawing;
legibility is simultaneous low-dimensional projections sharing the
cursor).

`probes/probe_asks.py` written and green (16 facts): region-floor
mechanism, altitude/orbit/interior/laterality costs and gaps, pass
composition. ASKS.md revision 1 folded the findings in.

Adversarial round run (Opus agent; `probes/ADVERSARIAL_ASKS.md` +
`probes/adv_asks.py`): 3 blockers · 11 majors · 8 minors. Author
conceded B1 (occurrence addresses need a top-down positional path
contract — the spine shares equal nodes by identity), B2 (rule-keyed
ceilings are formulation-bound; canonical names, minimal declaration,
gated), B3 (the keystone splits — addressed emission is lexic's, the
geometry solve is opsis's), and M1–M6, M8–M11 with the m-series; ASKS.md
rewritten as revision 2 (seven asks: addressed emission+provenance;
ceiling contract; verdict record + identity walk; seam family; compile
moments through `_assemble_core`; kernel trace; generate refusals),
VISION §4/§5 and QUESTIONS Q3/Q4/Q6 repaired. One dissent held for
arbitration: M7's full merge — author keeps the kernel trace separate
from the correspondence-record ask (event stream vs correspondence;
one vocabulary over both is speculative generality).

## 2026-08-16 — effort opened

Created from the ruling conversation that reframed 260807-opsis-radical's
open questions: lexic fully in scope, the cycle as navigable geometry,
universal rendering as the claim, opsis destined for src, no sacred cows.
First drafts of README / VISION / ASKS / QUESTIONS written, grounded
against `src/lexic` (transpile, templating, layout, generate, artifact,
model, flavour surfaces read; `export_value` seam gap and `SpanEntry`
text-not-offsets verified) and the prototype corpus (space_3 code,
REVIEW_SPACE3_IMPL, ADVERSARIAL_MOCK, WAYFORWARD, visual_4, the 2607xx
architecture concept). Awaiting first review pass with the user; no src
work started.
