"""The spectacle — one reading drawn whole, in pixels ready to paint.

The frame is the only thing this build sends: final coordinates, named
tones, and the rectangles a pointer can land on. Nothing downstream
decides anything, so nothing downstream can disagree.
"""
