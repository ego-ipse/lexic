Unit mirrors for `src/lexic/parsing/parallel/plan`.

Envelope reading, the noise-run lead, and cut derivation/normalization over
raw text — the shape questions a split answers before anything is parsed.
