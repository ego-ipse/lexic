Unit mirrors for `src/lexic/parsing/parallel/stitch`.

Safety tests prove that separator ownership is conservative before stitching.
`support.py` shares the exact split fixture used at the model and merge seams.
