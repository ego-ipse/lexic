[project]
name = "vyx-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.12.5",
    "pynvml>=13.0.1",
    "pyparsing>=3.3.2",
    "rich>=14.3.3",
    "ruff>=0.15.9",
]

[dependency-groups]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
