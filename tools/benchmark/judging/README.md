# Benchmark judging

A row's verdict and the arithmetic that decides it: the paired log ratios, the
interval, the control envelope and the status. Every tier that judges a row
reads its numbers from here; scheduling the pairs stays with `compare.py`.
